import { Card,  CardContent, Grid, Typography } from "@mui/material";
const StudentList = () => {
  return <div>
    <Card sx={{ p: 150 }}>
      <CardContent>

      </CardContent>
    </Card>
  </div>;
};

export default StudentList;
